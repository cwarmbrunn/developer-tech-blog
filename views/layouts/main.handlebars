<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Tech Blog</title>
        {{!--Google Fonts--}}
        <style>
            @import url('https://fonts.googleapis.com/css2?family=Chivo&family=Lora:wght@700&family=Open+Sans:wght@600&display=swap');
        </style>
        <link rel="stylesheet" href="/stylesheets/style.css">
            {{!--Favicon--}}
            <link rel="icon" type="image/x-icon" href="/assets/favicon.ico"/>
   
    </head>

    <body>
        <div class="wrapper">
            <header>
                <h1>
                    <a href="/"> Tech Blog</a>
                </h1>
                <nav>
                    {{#if loggedIn}}
                    <a href="/dashboard">Dashboard</a>
                    <button id="logout" class="btn-no-style"> Logout</button>
                    {{else}}
                    <a href="/login">Login</a>
                    {{/if}}
                </nav>
            </header>
            <main>
                {{{ body }}}
            </main>
            <footer>
                Made by Christina Warmbrunn ✨
            </footer>
        </div>
        {{#if loggedIn}}
        <script src="/javascript/logout.js"></script>
        {{/if}}
    </body>
</html>